#!/bin/sh

branch="$(git symbolic-ref --quiet --short HEAD 2>/dev/null || true)"

if [ "$branch" = "main" ]; then
  echo "Direct commits to main are blocked. Switch to dev or another working branch first." >&2
  exit 1
fi

exit 0
